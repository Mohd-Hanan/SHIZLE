kind = "web"
previewPath = "/"
title = "SHIZLE"
version = "1.0.0"
id = "artifacts/shizle"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20282

[services.development]
run = "pnpm --filter @workspace/shizle run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/shizle", "run", "build" ]
publicDir = "artifacts/shizle/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20282"
BASE_PATH = "/"
